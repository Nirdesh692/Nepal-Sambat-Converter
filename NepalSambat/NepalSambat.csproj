<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    
    <!-- NuGet Package Metadata -->
    <PackageId>NepalSambat</PackageId>
    <Version>1.1.0</Version>
    <Authors>NepalSambat Contributors</Authors>
    <Company>NepalSambat Open Source</Company>
    <Description>A high-precision, astronomical-based Nepal Sambat calendar converter library. Supports bidirectional conversion between Gregorian and Nepal Sambat dates with 99.5% accuracy against ground truth.</Description>
    <Copyright>Copyright © 2025</Copyright>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageProjectUrl>https://github.com/nirdesh/NepalSambat</PackageProjectUrl>
    <RepositoryUrl>https://github.com/nirdesh/NepalSambat</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageTags>nepal-sambat;calendar;converter;lunar;astronomical;nepal;date-conversion</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
    
    <AssemblyTitle>NepalSambat Calendar Converter</AssemblyTitle>
    <AssemblyDescription>High-precision Nepal Sambat calendar converter</AssemblyDescription>
    <AssemblyVersion>1.1.0.0</AssemblyVersion>
    <FileVersion>1.1.0.0</FileVersion>
  </PropertyGroup>

  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="/" />
  </ItemGroup>

</Project>
